import Alarm from "./Alarm"; 

describe("Alarm", function () {
    test("fixme", function () {
        const alarm = new Alarm();
        alarm.check();
        expect(false).toEqual(true);
    });
});